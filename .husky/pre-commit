branch=$(git rev-parse --abbrev-ref HEAD)

if [ "$branch" = "main" ]; then
  echo "❌ Direct commits to main are not allowed."
  exit 1
fi

pnpm run lint-staged
pnpm run type:check
# pnpm test # in-future